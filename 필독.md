## 🚀 팀원 환경 세팅 가이드

### 사전 준비

- Python 3.x 설치 (https://www.python.org)
- Git 설치 (https://git-scm.com)
- VS Code 설치 + 다음 확장 설치
  - Python (Microsoft)
  - Jupyter (Microsoft)
  - Claude Code (Anthropic, 선택)

---

### 1. 저장소 클론

원하는 위치(예: 바탕화면)에서 터미널 열고:

```bash
git clone https://github.com/본인아이디/steam_discount_analysis.git
cd steam_discount_analysis
code .
```

VS Code가 새 창으로 열리면, 이 창에서 터미널 다시 열기 (`Ctrl+` ` ` 백틱).

---

### 2. 가상환경 생성

```bash
python -m venv venv
```

> 만약 `python` 명령어가 안 먹으면 `python3` 또는 `py`로 시도해보세요.

---

### 3. 가상환경 활성화

**Windows (PowerShell):**

```powershell
venv\Scripts\Activate.ps1
```

> 권한 에러 나면 한 번만 아래 명령어 실행 후 다시 시도:
>
> ```powershell
> Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
> ```

**Windows (cmd):**

```cmd
venv\Scripts\activate.bat
```

**Mac/Linux:**

```bash
source venv/bin/activate
```

→ 터미널 줄 앞에 `(venv)` 가 붙으면 성공.

---

### 4. 라이브러리 설치

```bash
pip install -r requirements.txt
```

> 만약 `requirements.txt`에 문제가 있으면 수동 설치:
>
> ```bash
> pip install requests pandas numpy matplotlib seaborn python-dotenv tqdm jupyter ipykernel
> ```

설치 확인:

```bash
pip list
```

---

### 5. `.env` 파일 생성

저장소에는 `.env`가 없어요 (보안상 GitHub에 안 올림). 직접 만들어야 해요.

프로젝트 루트에 `.env` 파일 만들고 내용 붙여넣기:

```
ITAD_API_KEY=여기에_API_키
STEAM_API_KEY=여기에_API_키
```

API 키는 팀원에게 별도로 공유받으세요 (카톡 등).

---

### 6. Jupyter 커널 선택

1. `notebooks/` 폴더에서 `.ipynb` 파일 아무거나 열기
2. 오른쪽 위 **"Select Kernel"** 클릭
3. **Python Environments** → **`venv (Python 3.x.x)`** 선택

---

### 7. 작동 테스트

노트북 첫 셀에 입력하고 `Shift+Enter`로 실행:

```python
import requests
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from dotenv import load_dotenv
import os

load_dotenv()
print("✅ 환경 세팅 완료")
print(f"ITAD_API_KEY 로드 확인: {'OK' if os.getenv('ITAD_API_KEY') else '❌ .env 파일 확인'}")
```

에러 없이 출력되면 끝!

---

### ✅ 세팅 체크리스트

- [ ] Python, Git, VS Code 설치
- [ ] 저장소 클론 + VS Code로 열기
- [ ] 가상환경 생성 (`venv` 폴더 생김)
- [ ] 가상환경 활성화 (터미널에 `(venv)` 표시)
- [ ] `pip install -r requirements.txt` 완료
- [ ] `.env` 파일 직접 생성 + API 키 입력
- [ ] Jupyter 커널을 `venv`로 선택
- [ ] 첫 셀 실행 → 에러 없음

````

---

## 한눈에 요약 (팀원이 봐야 할 핵심만)

```bash
# 1. 클론
git clone https://github.com/본인아이디/steam_discount_analysis.git
cd steam_discount_analysis
code .

# 2. 가상환경 (새로 연 VS Code 터미널에서)
python -m venv venv

# 3. 활성화 (Windows)
venv\Scripts\Activate.ps1

# 3. 활성화 (Mac/Linux)
source venv/bin/activate

# 4. 라이브러리 설치
pip install -r requirements.txt

# 5. .env 파일 직접 만들고 API 키 붙여넣기
# 6. 노트북 열어서 커널 선택 (venv)
# 7. 첫 셀 실행 테스트
```

---

## ⚠️ 자주 막히는 포인트

1. **`python` 명령어 인식 안 됨**
   → `python3` 또는 `py`로 시도. 안 되면 Python 설치 시 "Add to PATH" 체크 안 한 것. Python 재설치하면서 체크.

2. **PowerShell 활성화 권한 에러**
   → 위에 적어둔 `Set-ExecutionPolicy` 명령어 한 번 실행

3. **`pip install -r requirements.txt`에서 에러**
   → 보통 Python 버전 호환 문제. 수동 설치 명령어로 시도.

4. **노트북에서 import 에러**
   → 99% 커널 선택 실수. Select Kernel에서 `venv` 선택했는지 확인.

5. **`.env` 파일이 안 보임**
   → Windows 탐색기에서 숨김 파일 표시 켜기. VS Code 파일 탐색기에서는 잘 보임.

---

## API 키 공유 방법

**절대 GitHub에 올리지 마세요.** `.env`는 `.gitignore`에 들어있어서 안 올라가는 게 정상이에요. 팀원에게는:

- 카카오톡 비밀 채팅
- 또는 Google Drive 비공개 메모
- 또는 직접 만나서 전달

이런 식으로 따로 전달하세요.

---

## 정리

본인이 해야 할 일:
1. 가상환경 활성화 상태에서 `pip freeze > requirements.txt` 한 번 실행
2. `requirements.txt`를 GitHub에 푸시
3. 위 가이드를 `README.md`에 추가해서 같이 푸시
4. 팀원에게 API 키 따로 전달

팀원이 해야 할 일:
- README.md 보고 그대로 따라하면 됨

이러면 팀원이 막힐 일 거의 없어요. 혹시 막히면 이 메시지에 적은 "자주 막히는 포인트"가 거의 다 커버해줄 거예요.
````

---

## 대용량 데이터 파일 안내

`data/review_individual.csv`는 약 350MB 대용량 파일이라 GitHub에 포함하지 않았습니다.

- **전체 재현 시**: `notebooks/05b_reviews_individual.ipynb`를 실행해 생성 (Steam API 호출, 수 시간 소요)
- **빠른 구조 확인용**: `data/review_individual_test.csv` (샘플 3개 게임, 300개 리뷰) 사용 가능

`data/analysis_df.csv`와 `data/*.csv` 나머지 파일들은 GitHub에 포함되어 있으므로 `07_analysis.ipynb`와 `09_statistical_validation.ipynb`는 바로 실행 가능합니다.
